{
	"name": "megacenter-api",
	"license": "UNLICENSED",
	"private": true,
	"scripts": {
		"server": "pm2 delete init && pm2 start init.js --watch --ignore-watch='.git/*' && pm2 logs"
	},
	"dependencies": {
		"express": "^4.21.2",
		"soap": "~1.0.0"
	}
}
